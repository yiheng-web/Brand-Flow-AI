---
description: 全仓 TypeScript、命名、错误处理与可维护性规范
globs: **/*.{ts,tsx,js,mjs,cjs}
alwaysApply: false
---

# TypeScript 与代码质量

- 以根目录 ESLint、Prettier 和 `Coding.md` 为准：2 空格、单引号、无分号、100 字符换行、尾逗号。
- 只格式化实际修改范围，不借任务大面积整理历史文件。
- 变量/函数使用 `camelCase`，类型/类/组件使用 `PascalCase`，稳定配置常量使用 `UPPER_SNAKE_CASE`；命名表达业务含义。
- 类型导入使用 `import type`；导入按外部依赖、内部别名、相对路径和样式分组。
- Props、DTO、API/Agent 公共入参与返回值必须显式类型化。未知数据使用 `unknown` 并校验，不用 `any`、断言或忽略规则掩盖不确定性。
- 异步逻辑使用 `async/await`；捕获的异常先收窄类型，保留业务上下文并交给既有错误处理，禁止空 `catch`。
- 函数保持单一职责；只在真实复用或复杂度需要时抽象，不创建无意义包装、重复工具或万能 common 文件。
- 注释使用中文，只说明业务原因、边界和非显然决策；删除失效注释及长期注释掉的旧代码。
- 不硬编码密钥、生产 endpoint、本机路径、模型配置和可变业务阈值；使用现有环境变量或具名配置入口。
- 修改公共类型、状态枚举或跨包导出时，搜索所有消费者并同步契约、文档和验证。
