---
description: apps/web CSS Modules、设计变量与第三方组件样式规范
globs: apps/web/src/**/*.{css,less,tsx}
alwaysApply: false
---

# Web 样式规则

- 业务页面和组件优先使用 `*.module.css` / `*.module.less`，通过 `styles.xxx` 引用，避免新增无边界全局样式。
- `src/index.css` 只承载基础变量、reset 和确需全局生效的第三方基础样式；新增品牌颜色、阴影和圆角先复用现有 CSS 变量。
- 常规布局、间距和静态视觉使用 class，动态坐标、运行时尺寸及第三方组件必要参数可使用内联样式。
- Ant Design、React Flow、Fabric 样式覆盖必须限定在当前页面或组件容器内，禁止裸写全局标签或第三方内部 class。
- 保持现有信息层级、间距、圆角和交互反馈，不因局部需求重做全局设计系统。
- 样式命名表达组件结构和业务语义；同一模块保持既有命名风格，不混入无意义缩写。
- hover、focus、active、disabled、loading 和错误状态保持可区分；不要只靠颜色表达状态。
- 检查窄屏、长文本、空数据和滚动容器，避免固定尺寸导致内容截断；画布类固定高度需确认父容器高度链路。
- 动画用于状态反馈，避免在高频画布交互中添加昂贵阴影、滤镜和大范围过渡。
