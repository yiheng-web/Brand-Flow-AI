---
description: Brand-Flow AI 全仓执行流程、Monorepo 边界与交付要求
alwaysApply: true
---

# 项目核心规则

- 先阅读根目录 `AGENTS.md`、`Coding.md`、相关包的 `package.json` 和任务调用链；以实际代码、配置与文档共同确认行为。
- 修改前说明需求理解、涉及模块、复用方案、风险和验证计划；检查 `git status`，保护用户已有改动。
- 遵循最小必要修改，不重构无关代码、不更换技术栈、不大范围格式化、不删除兼容逻辑。
- 包边界固定：Web 只调用 API；API 负责 HTTP、鉴权、租户、持久化、队列和基础设施；`packages/agent` 只承载 AI 库逻辑。
- 使用 Node.js 20+、仓库锁定的 pnpm 和 Turbo 脚本；不要使用 npm/Yarn，不手改生成目录或锁文件内容。
- 所有文本使用 UTF-8，代码注释使用简洁中文；密钥、Token、密码、本地绝对路径和生产配置不得写入仓库。
- 新增代码优先复用现有组件、Store、API 客户端、DTO、异常体系、Service、工具和公共导出。
- 不用硬编码、伪数据、吞异常、关闭类型检查、跳过测试或删除断言制造“已完成”。
- 验证按改动范围执行：先包级 lint/test/build，再按风险扩大；规则/文档改动检查 frontmatter、链接、UTF-8 和 `git diff --check`。
- 未真实执行的测试不得写成通过；阻塞时说明原因、替代验证和用户后续命令。
- 不自动提交或推送。未经明确授权禁止历史重写、强推、清理工作区和不可逆操作。
